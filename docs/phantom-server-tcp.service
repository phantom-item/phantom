[Unit]
Description=Phantom Proxy Server (TCP/TLS)
After=network.target nginx.service

[Service]
Type=simple
ExecStart=/usr/local/bin/phantom-server --config /etc/phantom/config.json
Restart=on-failure
RestartSec=5s
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
