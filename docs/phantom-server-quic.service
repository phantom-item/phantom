[Unit]
Description=Phantom Proxy Server (QUIC)
After=network.target nginx.service

[Service]
Type=simple
ExecStart=/usr/local/bin/phantom-server --quic --config /etc/phantom/config.json
Restart=on-failure
RestartSec=5s
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
